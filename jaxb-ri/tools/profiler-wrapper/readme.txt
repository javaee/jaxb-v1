
This sub-project defines a wrapper around the external profiling tool (such as OptimizeIt), so that the performance test harness can control those tools.

To compile this project, you need to create the profiler-wrapper/lib directory and copy "optit.jar" from $OPTIMIZEIT_HOME/lib.
